//services: En esta carpeta se centralizaría toda la lógica de comunicación con la API.
//productApi.js: Un archivo dedicado a exportar funciones que interactúan con tu API. 
//Por ejemplo, tendrías funciones como getProducts(), createProduct(data), updateProduct(id, data) y deleteProduct(id). 
//Esto hace que el código sea más limpio, ya que la lógica de la API está en un solo lugar.